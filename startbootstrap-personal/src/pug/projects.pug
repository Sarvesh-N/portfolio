doctype html
html(lang="en")
  head
    meta(charset="utf-8")
    meta(name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no")
    meta(name="description" content="")
    meta(name="author" content="")
    title Project
    // Favicon
    link(rel="icon" type="image/x-icon" href="assets/favicon.ico")
    // Custom Google font
    link(rel="preconnect" href="https://fonts.googleapis.com")
    link(rel="preconnect" href="https://fonts.gstatic.com" crossorigin)
    link(href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@100;200;300;400;500;600;700;800;900&display=swap" rel="stylesheet")
    // Bootstrap icons
    link(href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.8.1/font/bootstrap-icons.css" rel="stylesheet")
    // Core theme CSS (includes Bootstrap)
    link(href="css/styles.css" rel="stylesheet")
    style.
      .project-image {
        width: 100%;
        height: auto;
        max-height: 600px; /* Set a maximum height */
        transition: transform 0.3s ease, left 0.3s ease, top 0.3s ease;
        position: relative;
        object-fit: cover; /* Ensure the image covers the area */
        cursor: pointer; /* Change cursor to pointer */
      }
      .project-image:hover {
        transform: scale(1.1);
        left: 10px;
        top: 10px;
      }
      .project-card {
        transition: transform 0.3s ease, box-shadow 0.3s ease;
      }
      .project-card:hover {
        transform: scale(1.05);
        box-shadow: 0 10px 20px rgba(0, 0, 0, 0.2);
      }
      .fullscreen-overlay {
        display: none;
        position: fixed;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        background-color: rgba(0, 0, 0, 0.9);
        justify-content: center;
        align-items: center;
        z-index: 1000;
      }
      .fullscreen-overlay img {
        max-width: 90%;
        max-height: 90%;
      }
      .fullscreen-overlay .close-btn {
        position: absolute;
        top: 20px;
        right: 20px;
        font-size: 30px;
        color: white;
        cursor: pointer;
      }
  body.d-flex.flex-column.h-100.bg-light
    main.flex-shrink-0
      // Navigation
      nav.navbar.navbar-expand-lg.navbar-light.bg-white.py-3
        .container.px-5
          a.navbar-brand(href="index.html")
            span.fw-bolder.text-primary PROJECT
          button.navbar-toggler(type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation")
            span.navbar-toggler-icon
          .collapse.navbar-collapse#navbarSupportedContent
            ul.navbar-nav.ms-auto.mb-2.mb-lg-0.small.fw-bolder
              li.nav-item
                a.nav-link(href="index.html") Home
              li.nav-item
                a.nav-link(href="resume.html") Resume
              li.nav-item
                a.nav-link(href="projects.html") Projects
              li.nav-item
                a.nav-link(href="contact.html") Contact
      // Projects Section
      section.py-5
        .container.px-5.mb-5
          .text-center.mb-5
            h1.display-5.fw-bolder.mb-0
              span.text-gradient.d-inline Projects
          .row.gx-5.justify-content-center
            .col-lg-11.col-xl-9.col-xxl-8
              // Project Card 1
              .card.overflow-hidden.shadow.rounded-4.border-0.mb-5.project-card
                .card-body.p-0
                  .d-flex.align-items-center
                    .p-5
                      h2.fw-bolder Spam Detection Using AI
                      p It’s a Machine Learning project that utilizes a pre-trained dataset. Users can input a message in the text field, and the system classifies it as either spam or not, It uses Streamlit for the frontend.
                      a.btn.btn-outline-primary(href="https://github.com/Sarvesh-N/spam_detection" target="_blank") View on GitHub
                    //img.img-fluid.project-image(src="project_images/spam.png" alt="..." onclick="openFullscreen(this)")
              // Project Card 2
              .card.overflow-hidden.shadow.rounded-4.border-0.mb-5.project-card
                .card-body.p-0
                  .d-flex.align-items-center
                    .p-5
                      h2.fw-bolder Conversatinal Image Recogonization Chatbot
                      p It is a project that recognizes the image uploaded by the user with BLIP and provides a description based on the user's query using Llama2 , Uses Streamlit for the frontend. The system integrates image recognition and natural language processing to deliver accurate responses.
                      a.btn.btn-outline-primary(href="https://github.com/Sarvesh-N/converstational_image_recogonaiization_chatbot" target="_blank") View on GitHub
                    //img.img-fluid.project-image(src="project_images/bot.png" alt="..." onclick="openFullscreen(this)")
              // Project Card 3
              .card.overflow-hidden.shadow.rounded-4.border-0.mb-5.project-card
                .card-body.p-0
                  .d-flex.align-items-center
                    .p-5
                      h2.fw-bolder Site Seeker
                      p This project utilizes BeautifulSoup to scrape data from the web based on the user's input. It searches across various websites related to the prompt, analyzes the collected data, and visualizes the results by plotting a bar chart using Matplotlib and uses Streamlit for the UI .
                      a.btn.btn-outline-primary(href="https://github.com/Sarvesh-N/Site_seeker" target="_blank") View on GitHub
                    //img.img-fluid.project-image(src="project_images/web_scrapper.png" alt="..." onclick="openFullscreen(this)")
      // Fullscreen Overlay
      .fullscreen-overlay#fullscreenOverlay
        span.close-btn(onclick="closeFullscreen()") &times;
        img#fullscreenImage(src="" alt="Fullscreen Image")
      // Call to action section
      section.py-5.bg-gradient-primary-to-secondary.text-white
        .container.px-5.my-5
          .text-center
            h2.display-4.fw-bolder.mb-4 Let's build something together
            a.btn.btn-outline-light.btn-lg.px-5.py-3.fs-6.fw-bolder(href="contact.html") Contact me
    // Footer
    footer.bg-white.py-4.mt-auto
      .container.px-5
        .row.align-items-center.justify-content-between.flex-column.flex-sm-row
          // .col-auto
          //   .small.m-0 Copyright &copy; Your Website 2023
          // .col-auto
          //   a.small(href="#!") Privacy
          //   span.mx-1 &middot;
          //   a.small(href="#!") Terms
          //   span.mx-1 &middot;
          //   a.small(href="#!") Contact
    // Bootstrap core JS
    script(src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/js/bootstrap.bundle.min.js")
    // Core theme JS
    script(src="js/scripts.js")
    script.
      function openFullscreen(img) {
        var overlay = document.getElementById('fullscreenOverlay');
        var fullscreenImg = document.getElementById('fullscreenImage');
        fullscreenImg.src = img.src;
        overlay.style.display = 'flex';
      }

      function closeFullscreen() {
        var overlay = document.getElementById('fullscreenOverlay');
        overlay.style.display = 'none';
      }

      document.addEventListener('keydown', function(event) {
        if (event.key === 'Escape') {
          closeFullscreen();
        }
      });
